globals:
  - id: soil_probe_version
    type: sensasoil::SoilProbeVersion
    restore_value: yes
    initial_value: "sensasoil::SoilProbeVersion::V3"
    update_interval: ${SAVE_TO_STORAGE_USER_CONFIG_INTERVAL}
  # INTERNAL #
  - id: power_sensors_time
    type: unsigned long
    restore_value: no
    initial_value: "0"

substitutions:
  ADC_SAMPLES: "5"

sensor:
  - platform: dht
    pin: GPIO5
    model: DHT22
    id: temperature_humidity_sensor
    update_interval: never
    temperature:
      id: ambient_temperature
      internal: true
    humidity:
      id: ambient_humidity
      internal: true
  - platform: adc
    id: battery_voltage
    pin: GPIO3
    attenuation: 12db
    samples: 32
    filters:
    - calibrate_linear:
          - 1.91 -> 3.3
          - 2.40 -> 4.2
    - median:
        window_size: ${ADC_SAMPLES}
        send_every: ${ADC_SAMPLES}
        send_first_at: ${ADC_SAMPLES}
    update_interval: never
    internal: true
  - platform: template
    id: battery_level
    lambda: !lambda |-
      const float battery = id(battery_voltage).state;
      if (battery <= 3.3f) {
        return 0.0f;
      }
      if (battery >= 4.2f) {
        return 100.0f;
      }
      return (battery - 3.3f) * (100.0f / (4.2f - 3.3f));
    update_interval: never
    internal: true
  - platform: adc
    id: soil_temperature_v
    attenuation: 12db
    samples: 32
    pin: GPIO1
    update_interval: never
    internal: true
    filters:
      - median:
          window_size: ${ADC_SAMPLES}
          send_every: ${ADC_SAMPLES}
          send_first_at: ${ADC_SAMPLES}
  - platform: adc
    id: soil_volumetric_water_v
    attenuation: 12db
    samples: 32
    filters:
      - median:
          window_size: ${ADC_SAMPLES}
          send_every: ${ADC_SAMPLES}
          send_first_at: ${ADC_SAMPLES}
    pin: GPIO4
    update_interval: never
    internal: true

binary_sensor:
  - platform: gpio
    id: is_battery_charging
    trigger_on_initial_state: true
    use_interrupt: true
    pin:
      number: GPIO6
      mode: 
        input: True
        pullup: True
      inverted: True
    internal: True
  - platform: template
    id: is_solar_charging  # Unused, but kept for compatibility with newer hardware versions
    internal: true
    trigger_on_initial_state: true
  - platform: template
    id: is_plugged_in_charging  # Unused, but kept for compatibility with newer hardware versions
    internal: true
    trigger_on_initial_state: true
  - platform: gpio
    id: factory_reset_gpio_button
    trigger_on_initial_state: true
    use_interrupt: True
    pin:
      number: GPIO9
      ignore_strapping_warning: true
      mode:
        input: True
        pullup: True
      inverted: True
    internal: True
    filters:
      - delayed_on: 15s
      - delayed_off: 10ms
    on_press:
      then:
        - button.press: factory_reset_button

output:
  - platform: ledc
    id: led_status_output
    pin: GPIO0

switch:
  - platform: gpio
    id: power_sensors
    pin: GPIO10
    icon: mdi:electric-switch
    name: "Power sensors"
    restore_mode: ALWAYS_OFF
    internal: True
    on_turn_on:
      then:
        - lambda: ! id(power_sensors_time) = millis();

script:
  - id: read_sensors_specific
    mode: restart
    then:
      # Battery charging is automatically updated by the GPIO
      - repeat:
          count: ${ADC_SAMPLES}
          then:
          - component.update: battery_voltage
          - delay: 1ms # yield
      - component.update: battery_level
      # Update peripherals voltage sensors
      # Turn on peripheral power if not already
      - lambda: |-
          // The if is important to prevent resetting the power_sensors_time
          if (!id(power_sensors).state) {
            id(power_sensors)->turn_on();
          }
      # Wait (hardware dependent delay)
      - delay: !lambda |-
          const unsigned long elapsed = millis() - id(power_sensors_time);
          return elapsed < 80 ? 80 - elapsed : 0;
      - repeat:
          count: ${ADC_SAMPLES}
          then:
          - component.update: soil_volumetric_water_v
          - delay: 1ms # yield
      # Wait (hardware dependent delay)
      - delay: !lambda |-
          const unsigned long elapsed = millis() - id(power_sensors_time);
          return elapsed < 100 ? 100 - elapsed : 0;
      - repeat:
          count: ${ADC_SAMPLES}
          then:
          - component.update: soil_temperature_v
          - delay: 1ms # yield
      # Wait (hardware dependent delay)
      - delay: !lambda |-
          const unsigned long elapsed = millis() - id(power_sensors_time);
          return elapsed < 150 ? 150 - elapsed : 0;
      - component.update: temperature_humidity_sensor
      - delay: 1ms # yield
      # Turn off peripheral power
      - switch.turn_off: power_sensors
